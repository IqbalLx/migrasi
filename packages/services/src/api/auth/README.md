# services-src-api-auth

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build services-src-api-auth` to build the library.

## Running unit tests

Run `nx test services-src-api-auth` to execute the unit tests via [Jest](https://jestjs.io).
